---
title: CNN 手写数字识别与实验（MNIST）
date: 2026-06-08
updated: 2026-06-09
tags:
  - AI
  - 机器学习
  - CNN
  - MNIST
  - 卷积神经网络
  - 学习笔记
aliases:
  - MNIST CNN 学习笔记
  - 神经网络学习笔记 MLP与CNN
source: C:/work/code/others/neuralnetworks/cnn
related: "[[2026-06-08 MLP 手写数字识别与优化]]"
vault_path: personal/technology/aiml/study
---

# CNN 手写数字识别与实验 · 2026-06-08（同步更新 2026-06-09）

> [!info] 与 MLP 笔记的分工
> **MLP** 的推导、超参实验、错误分析与增强鲁棒性（全连接视角）已写在：[[2026-06-08 MLP 手写数字识别与优化]]。  
> **本文记录 CNN**：**双卷积块**结构、默认训练配置、测试集结果、与 MLP 对照及鲁棒性实验说明。

> [!summary] 一页总览
> 纯 NumPy **双块**结构：`Conv(3×3,F=32)→ReLU→MaxPool → Conv(3×3,F₂=64)→ReLU→MaxPool`（28→14→7）→ **Flatten 3136** → FC → Softmax。同一划分（前 5 万训练 / 后 1 万测试），默认 **`train_eval.py`：10 epoch、`batch_size=512`、Dropout 0.05** 时测试集约 **98.74%**（126 / 10000 错），优于优化 MLP **97.86%**。继续加 epoch 可向 **99%** 推进。  
> **代码**：`C:/work/code/others/neuralnetworks/cnn` · **本页配图**：与本笔记同目录 `2026-06-08_cnn_*.png`（由仓库 `cnn/images/` 同步）。

---

## 1. 工程与数据

- 数据集：仓库根目录 `neuralnetworks/data/mnist.npz`（与 `mlp/` 共用）。
- 在 `cnn/` 下运行脚本，数据路径 `../data/mnist.npz`。
- 展示图：仓库内 `cnn/images/`；可重生成产物 `cnn/out/`。Obsidian 侧已复制一批同名前缀图到 **本目录**（见上文）。

---

## 2. 网络结构

![CNN 结构示意图（双卷积块）](2026-06-08_cnn_network_structure%201.png)

$$\text{Input}(1\times28\times28)\to\text{Block1: Conv3×3}(F{=}32)\to\text{ReLU}\to\text{MaxPool}\to\text{Block2: Conv3×3}(F_2{=}64)\to\text{ReLU}\to\text{MaxPool}\to\text{Flatten}(7{\times}7{\times}64)\to\text{FC}\to\text{Softmax}$$

- 卷积：im2col / col2im；same padding；He 初始化；第二块输入通道 = **F**。
- 优化：Adam `lr=1e-3`、L2 `1e-4`、Dropout **`0.05`**、学习率每 epoch ×0.97；默认 **10 epoch**、`batch_size=512`（以 `train_eval.py` 为准）。

---

## 3. 训练结果（前 50000 训练 / 后 10000 测试）

| 模型 | 测试准确率 | 错误数 |
|---|---|---|
| 优化 MLP（对照，见姊妹篇） | 97.86% | 214 / 10000 |
| **CNN (clean，双块默认配置)** | **98.74%** | **126 / 10000** |

![](2026-06-08_cnn_cnn_training%201.png)

![](2026-06-08_cnn_cnn_confusion_matrix%201.png)

![](2026-06-08_cnn_cnn_confident_errors%201.png)

> [!tip] 关于 99%
> 单卷积块在该划分上约 **98.4% 封顶**；已升级为 **双卷积块**。默认 **10 epoch** 约 **98.74%**；加长训练或微调学习率/容量可继续冲 **99%**。详见仓库 `cnn/README.md`。

---

## 4. 旋转 / 平移 / 缩放鲁棒性

运行 `augment_eval.py`：**CNN (clean)** 优先加载 `out/cnn_clean.npz`；**CNN (aug-trained)** 与 clean **结构相同**（双块），训练时 batch 内随机旋转/平移/缩放。**MLP (optimized)** 列仍为 `mlp/augment_test.py` 对照数据。

> [!warning] 表内 CNN 扰动列需本地刷新
> 下列 **rotate / shift / scale / combined** 及 **CNN (aug-trained)** 数字会随权重与脚本版本变化。请在 `cnn/` 下执行 `python augment_eval.py`，以终端或 `out/cnn_augment_report.txt` 为准更新下表。

| 增强 | CNN (clean) | CNN (aug-trained) | MLP (optimized) |
|---|---|---|---|
| clean | **98.74%** | *运行 augment_eval* | 97.86% |
| rotate | *待刷新* | *待刷新* | 96.94% |
| shift | *待刷新* | *待刷新* | 70.17% |
| scale | *待刷新* | *待刷新* | 72.46% |
| combined | *待刷新* | *待刷新* | 57.54% |

![](2026-06-08_cnn_cnn_augmentation_examples%201.png)

![](2026-06-08_cnn_cnn_augmentation_robustness%201.png)

> [!success] 结论（定性）
> 卷积 + 池化带来一定平移/尺度鲁棒性；**训练时增强**可显著提升旋转/平移/缩放下的表现，通常以略降 clean 分为代价。定量请用本机 `augment_eval.py` 输出更新上表与柱状图。

---

## 5. 补充：犹豫型错误样本

![](2026-06-08_cnn_cnn_ambiguous_errors%201.png)

---

## 6. TODO

- [x] 第二卷积块（28→14→7）已落地于 `cnn/cnn.py`
- [ ] 继续加 epoch / 调参，冲 MNIST **99%+**
- [ ] 重跑 `augment_eval.py`，把 §4 表格与柱状图数字填实
- [ ] 参数量 / 推理耗时对比表
- [ ] 更丰富的增强（如弹性形变）

## 参考

- 姊妹篇（MLP）：[[2026-06-08 MLP 手写数字识别与优化]]
- 仓库文档：`cnn/README.md`、`mlp/README.md`
- 技能：`.cursor/skills/nn-project-builder/SKILL.md`
