# Skills under `doc/ai/.cursor/skills`

This directory is reserved for **documentation-repo-local** skills (macros, ticket helpers, etc.).

For **L2-PS / U-Plane implementation** work, use the skills under the gNB workspace (for example `/workspace/.cursor/skills/` and `/workspace/uplane/.agents/skills/`) as described in `/workspace/AGENTS.md`.

See also: `.cursor/agents/common-cursor-ghc-paths.agent.md`.
